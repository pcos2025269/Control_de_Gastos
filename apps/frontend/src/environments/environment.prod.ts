/**
 * Variables de entorno para el build de produccion.
 *
 * En un despliegue real, apiUrl deberia apuntar al dominio publico del
 * backend (por ejemplo https://api.miapp.com/api) en lugar de localhost.
 */

export const environment = {
  production: true,
  apiUrl: 'https://TU_DOMINIO_DE_API/api',
};
